class Solution {
public:
    string findDifferentBinaryString(vector<string>& nums) {
        string str;
        for(int i=0 ; i<nums.size() ; i++){
            if(nums[i][i] == '0')
                str += '1';
            else
                str += '0';
        }
        return str;
    }
};
